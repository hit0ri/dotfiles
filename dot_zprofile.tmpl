# ~/.zprofile - sourced for login shells only, after zshenv
# Load order: zshenv -> zprofile (login) -> zshrc (interactive) -> zlogin (login)
# Purpose: login-session setup that must run before zshrc (e.g. PATH or
# environment overrides specific to login shells, ssh sessions, tmux).
