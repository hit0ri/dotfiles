# ~/.zshenv - sourced for all shells (interactive, non-interactive, scripts, login)
# Load order: zshenv -> zprofile (login) -> zshrc (interactive) -> zlogin (login)
# Keep minimal: only variables needed by non-interactive shells and scripts.

export LANG=en_US.UTF-8

export EDITOR=nvim
export VISUAL=nvim
export SUDO_EDITOR=nvim
